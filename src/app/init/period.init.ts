/* eslint-disable no-console */

export const initPeriod = async () => {
    // const usersCount = await User.count({})
    // if (usersCount === 0) {
    //     const systemAdminRole = await Role.findOne({
    //         where: {
    //             name: DefaultValues.RoleAdminName,
    //         },
    //     })
    //     const now = new Date()
    //     const nextYearDate = new Date(now.setFullYear(now.getFullYear() + 1))
    //     await User.create({
    //         username: DefaultValues.UserAdminUsername,
    //         fullname: DefaultValues.UserAdminFullname,
    //         password: hashToken(DefaultValues.UserAdminPassword),
    //         role: systemAdminRole,
    //         expiration_date: nextYearDate.toDateString(),
    //     }).save()
    //     console.log("[✔] Admin user created")
    // }
}
